
    import { observable, computed, action } from "mobx";
    
    class Main {
      @observable
      id: number = 0;

      @observable
      name: string = "

      @action
      set setName(name: string){
        this.name = name
      }

      @computed
      get getName(){
        return this.name
      }
    }

    export default observable(Main)
    